/*
  store对象，集中管理状态，将state，action，reducers结合起来
 */
import React from 'react';
import ReactDOM from 'react-dom';

import App from './components/app';

ReactDOM.render(<App />, document.getElementById('app'));